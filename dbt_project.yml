name: 'videogame_dw'
version: '1.0.0'
config-version: 2

profile: 'videogame_dw'

model-paths: ["models"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  videogame_dw:
    staging:
      +schema: dw
      +materialized: view

    dimensions:
      +schema: dw
      +materialized: table

    facts:
      +schema: dw
      +materialized: table
models:
  staging:
    +schema: dw
    +materialized: view

  dimensions:
    +schema: dw
    +materialized: table

  facts:
    +schema: dw
    +materialized: table
